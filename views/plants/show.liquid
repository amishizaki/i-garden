{% layout 'layout.liquid' %}
{% block content %}
<div class="container-md">
    <article>
        <h2>
            {{ plant.name }}
        </h2>
        <h3>{{ plant.openfarm_data.attributes.binomial_name }}</h3>
        <img class="img-thumbnail" style="width: 100%; height: 200px;" src={{ plant.openfarm_data.attributes.main_image_path }} alt={{ plant.name }}>
        <h4>{{ plant.openfarm_data.attributes.description }}</h4>
        {%- comment -%} <h6>{{ plant.perennial? 'Perennial' :'Annual' }} </h6> {%- endcomment -%}
        <h4>{% if plant.perennial == 'true' %}
            Perennial
        {% else %}
            Annual
        {% endif %}
        </h4>
        <h5>Sun requirements: {{ plant.openfarm_data.attributes.sun_requirements }}</h5>
        {%- comment -%} should this be author? {%- endcomment -%}
        {% if user.owner == userId %}
            <div class="btn-group">
                <a href="/plants/{{ plant.name }}/edit" class="btn btn-primary">Edit</a>
                <form action="/plants/{{ plant.id }}?_method=DELETE" method="POST">
                    <input type="submit" value="Delete" class="btn btn-danger">
                </form>
            </div>
        {% endif %}
    </article>
</div>
{% endblock %}

{%- comment -%} keep this just in case {%- endcomment -%}
            {%- comment -%} {% if example.ready %}
            Ready
            {% else %}
            Not Ready
            {%endif%} {%- endcomment -%}