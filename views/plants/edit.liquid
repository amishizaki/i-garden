{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/plants/{{plant.id}}?method=PUT" method="POST">
        <fieldset>
            <legend>Edit {{plant.name}}</legend>
            <label class="form-label">Name: </label>
            <input 
                class="form-control"
                type="text" 
                name="name" 
                placeholder="{{ plant.name }}"
                value="{{ plant.name }}"
            >
            <label class="form-label">Scientific Name: </label>
            <input 
                class="form-control"
                type="text" 
                name="scientific_name" 
                placeholder="{{ plant.scientific_name }}"
                value="{{ plant.scientific_name }}"
            >
            <label class="form-label">Description:</label>
            <textarea 
                class="form-control"
                type="string" 
                name="description" 
                placeholder="{{ plant.description }}" 
                value="{{ plant.description }}" 
            ></textarea>
            <label class="form-label">Perennial or Annual:</label>
            <select class="form-select" aria-label="{{ plant.perennial }}" name="perrenial">
                <option selected>Perennial or Annual</option>
                <option value="Perennial">Perennial</option>
                <option value="Annual">Annual</option>
            </select>
            <label class="form-label">Sun conditions:</label>
            <select class="form-select" aria-label="Default select example" name="sun_requirement" aria-placeholder="{{ plant.sun_requirement }}">
                <option selected aria-placeholder="{{ plant.sun_requirement }}"></option>
                <option value="Full sun">Full sun</option>
                <option value="Part sun">Part sun</option>
                <option value="Part shade">Part shade</option>
                <option value="Full shade">Full shade</option>
            </select>
        </fieldset>
        <input type="submit" value="Submit Edits for {{plant.name}}" class="btn btn-success">
    </form>
</div>
{% endblock %}